console.log(soma(3,1))

//function declaration
function soma(x, y) { //pode ser chamada antes de declara
    return x + y
}

//function expression
const sub = function (x, y) {
    return x - y
}

//named function expression
const mult = function mult(x, y) {
    return x * y
}